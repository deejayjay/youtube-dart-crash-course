void main(List<String> args) {
  // Variable names - lower camel case
  // Good naming convention
  const studentNamesList = ['Deon', 'Xander', 'Patrick'];
  print(studentNamesList);

  // Bad naming convention (should be wow)
  final Wow = "wow";
  print(Wow);

  // Bad naming convention (should be someName)
  const SomeName = "Deepak";
  print(SomeName);
}
